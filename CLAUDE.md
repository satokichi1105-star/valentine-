# valentine-

バレンタイン用のシンプルなWebページ（`index.html` と同梱メディア）。
加えて、Claude Code関連のX投稿を週次で要約する仕組みを同居させている。

## 進行中の作業

**Xの情報の自動取得。**実装済みだが未検証で、残タスクがある。
作業を再開する前に **`docs/x-summary-handoff.md`（引継書）を読むこと。**
決定事項、残タスク、すでに検証済みで繰り返す必要のない調査結果がまとまっている。

- 実装: `.claude/skills/x-summary/`（`/x-summary` で手動実行）
- セットアップと運用: `docs/x-summary-setup.md`
- 引継書: `docs/x-summary-handoff.md`
