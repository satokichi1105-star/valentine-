# valentine-

バレンタイン用のシンプルなWebページ（`index.html` と同梱メディア）。
加えて、Claude Code関連のX投稿を週次で要約する仕組みを同居させている。

## 進行中の作業

**X週次サマリー機能。**着手中で未完了のタスクがある。
作業を再開する前に **`docs/handoff.md` を読むこと。**決定事項、残タスク、
すでに検証済みで繰り返す必要のない調査結果がまとまっている。

- 実装: `.claude/skills/x-summary/`（`/x-summary` で手動実行）
- セットアップと運用: `docs/x-summary-setup.md`
- 引き継ぎメモ: `docs/handoff.md`

## 注意

`index.html` と同階層のメディアファイル（`.heic` / `.MOV`）は個人的な
コンテンツを含む。このリポジトリの公開設定を変更するときは中身を確認すること。
